# Program to subtract one number from another
# Input reads in a string so we need to convert it into an int to perform subtractional operation
# Author: Nikolay Sezonov
 
x = int(input('Enter first number, please : '))
y = int(input('Enter second number, please : '))
result = x - y

print('{} minus {} is equals {}' . format(x,y,result))
